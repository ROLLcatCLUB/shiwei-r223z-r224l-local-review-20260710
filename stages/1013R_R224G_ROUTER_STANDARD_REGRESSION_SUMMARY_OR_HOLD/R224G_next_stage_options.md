# R224G Next Stage Options

stage_id: 1013R_R224G_ROUTER_STANDARD_REGRESSION_SUMMARY_OR_HOLD

R224G does not automatically start a next stage.

## Option A. Teacher Default Leakage Regression

Purpose:

- generate or inspect teacher default manuscript outputs using router traces;
- verify raw router fields do not leak into teacher-facing text.

Still blocked:

- runtime/model/prompt unless separately authorized.

## Option B. classroom_event_expansion Field Convergence

Purpose:

- decide which router-derived fields remain computed output;
- decide which stay ledger-only;
- decide whether any optional event metadata should be formalized.

## Option C. Cross-Grade / Real Unit Stress Test

Purpose:

- validate router behavior across grades, long units, short units, and different performance task depths.

## Option D. Hold Until Broader R223P/R224 Consolidation

Purpose:

- avoid over-developing the router line before broader standard consolidation.

## Option E. Formal UI / R97B Planning

Status:

- blocked until separately authorized.

Do not use R224G as an implicit UI authorization.

## Recommended Next

Hold publication. If continuing, choose a small non-UI regression step focused on teacher default leakage or field convergence.
